import React from "react";
import { Typography } from "@mui/material";

const VaultPage: React.FC = () => {
  return <Typography variant="body1">Vault content will appear here.</Typography>;
};

export default VaultPage;
