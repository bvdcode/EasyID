import React from "react";
import VaultPage from "./VaultPage";

const AppShell: React.FC = () => {
  // In future we can have sidebar/layout here; for now render vault
  return <VaultPage />;
};

export default AppShell;
